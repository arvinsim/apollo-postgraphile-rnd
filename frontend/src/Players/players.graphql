query Players {
    allPlayers {
        nodes {
            id
            name
        }
    }
}

query Scores {
    allScores {
        nodes {
            id
            score
            playerId
            dateCreated
        }
    }
}
